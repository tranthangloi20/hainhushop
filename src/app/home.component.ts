import {Component,OnInit} from '@angular/core';import {CommonModule,CurrencyPipe} from '@angular/common';import {SupabaseService} from './supabase.service';import {Product} from './models';@Component({standalone:true,imports:[CommonModule,CurrencyPipe],template:`<section class="hero"><div class="wrap"><small>HẢI NHƯ SHOP</small><h1>Hàng thiết yếu<br><em>cho mọi gia đình.</em></h1><p>Sản phẩm tiện dụng, giá hợp lý và được tuyển chọn cho cuộc sống mỗi ngày.</p></div></section><section class="wrap products"><div class="title"><div><small>SẢN PHẨM</small><h2>Mua sắm dễ dàng</h2></div><span>{{items.length}} sản phẩm</span></div><div class="grid"><article *ngFor="let p of items"><img [src]="p.image_url" [alt]="p.name"><div><h3>{{p.name}}</h3><p>{{p.description}}</p><strong>{{p.price|currency:'VND':'symbol':'1.0-0'}}</strong><button>Thêm vào giỏ</button></div></article></div><p class="empty" *ngIf="loading">Đang tải...</p></section>`})export class HomeComponent implements OnInit{items:Product[]=[];loading=true;constructor(private s:SupabaseService){}async ngOnInit(){try{this.items=await this.s.products()}finally{this.loading=false}}}
