import {Component,OnInit} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {SupabaseService} from './supabase.service';import {Product} from './models';@Component({standalone:true,imports:[CommonModule,FormsModule],template:`<section class="wrap page"><small>QUẢN TRỊ</small><h1>Quản lý sản phẩm</h1><div *ngIf="!auth" class="panel"><form (ngSubmit)="login()"><input type="email" name="email" [(ngModel)]="email" placeholder="Email admin" required><input type="password" name="password" [(ngModel)]="password" placeholder="Mật khẩu" required><button>Đăng nhập</button><p class="err" *ngIf="error">{{error}}</p></form></div><div *ngIf="auth" class="admin"><div class="panel"><h2>{{editing?'Sửa':'Thêm'}} sản phẩm</h2><form (ngSubmit)="save()"><input name="name" [(ngModel)]="form.name" placeholder="Tên sản phẩm" required><textarea name="description" [(ngModel)]="form.description" placeholder="Mô tả"></textarea><input type="number" name="price" [(ngModel)]="form.price" placeholder="Giá" required><input name="image_url" [(ngModel)]="form.image_url" placeholder="URL ảnh"><button>Lưu sản phẩm</button></form></div><div><button class="outline" (click)="logout()">Đăng xuất</button><div class="item" *ngFor="let p of items"><img [src]="p.image_url"><b>{{p.name}}</b><span>{{p.price|number}} ₫</span><button class="outline" (click)="edit(p)">Sửa</button><button class="danger" (click)="remove(p)">Xóa</button></div></div></div></section>`})export class AdminComponent implements OnInit{auth=false;email='';password='';error='';items:Product[]=[];editing=false;form:Product={name:'',description:'',price:0,image_url:''};constructor(private s:SupabaseService){}async ngOnInit(){this.auth=!!await this.s.user();if(this.auth)this.load()}async load(){this.items=await this.s.products()}async login(){const r=await this.s.login(this.email,this.password);if(r.error)this.error='Thông tin đăng nhập không đúng';else{this.auth=true;this.load()}}async logout(){await this.s.logout();this.auth=false}edit(p:Product){this.form={...p};this.editing=true}async save(){await this.s.save(this.form);this.form={name:'',description:'',price:0,image_url:''};this.editing=false;await this.load()}async remove(p:Product){if(p.id&&confirm('Xóa sản phẩm?')){await this.s.remove(p.id);await this.load()}}}
